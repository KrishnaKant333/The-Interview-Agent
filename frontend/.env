# Backend origin for live API mode. Leave empty to use the Vite dev-server proxy (/api → localhost:8000).
VITE_API_BASE_URL=http://localhost:8000

# Set to "false" to use the real backend; "true" (default) uses MockInterviewGateway.
VITE_USE_MOCK=false
