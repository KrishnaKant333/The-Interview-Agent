# Leave empty to use the Vite dev-server proxy for /api during local development.
# Set to your backend origin in production, e.g. https://api.example.com
VITE_API_BASE_URL=

# Set to "false" to use the real backend instead of the mock gateway.
VITE_USE_MOCK=true
